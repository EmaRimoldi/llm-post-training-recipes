# External Datasets Placeholder

This directory is reserved for large local datasets that should not be committed to git.

Typical examples:

- `mathqa/train.json`
- `gpqa/`
- `tuandunghcmut_coding-mcq-reasoning/`

The root `.gitignore` excludes everything in this folder except this README.
