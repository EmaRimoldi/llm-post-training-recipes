# Configs

This folder stores model-configuration snapshots grouped by experiment stage.

The files are useful for:

- checking model naming and publication conventions;
- reconstructing which Hugging Face repositories were paired with specific experiments;
- comparing configuration drift across different training stages.
